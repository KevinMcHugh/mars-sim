# <System name>

<!--
Copy this file to docs/<system>.md when you add a feature, fill in the sections,
and add a row to the index table in docs/README.md. Delete these comments.
Write for the next contributor: explain the decisions and the dead ends, not just
what the code does line by line.
-->

> Part of the [mars-sim documentation](./README.md).

## What it is

One or two sentences: what this system does and where it fits.

## Source

- `path/to/file.go` — what lives here.
- `path/to/file_test.go` — the tests that pin the behavior.

## How it works

The model and the moving parts. Name the key types and functions. A small diagram
or table often beats a paragraph.

## Why it is this way

The tradeoffs and the approaches that did **not** work. This is the section that
saves the next person the most time — do not skip it.

## Extending it

The intended path for the most likely next change ("adding an X is a table edit
in Y"). Call out invariants a change must preserve.

## Related

- [another-doc.md](./another-doc.md) — how it connects.
